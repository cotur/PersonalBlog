<% content_for :title, "Categories | Edit -> " + @category.title %>

<h1>Editing Category</h1>

<%= render 'form', category: @category %>

<%= link_to 'Show', @category %> |
<%= link_to 'Back', categories_path %>
